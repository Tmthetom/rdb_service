Úkoly do pøíštì:
 - Instead of trigger (kontrola prázdných øetìzcù, pokud je, nepøidávat, zjistit následující ID, nìjak to jde, identity increment, identity next value, nebo nìco takového)
 - Logovací trigger (logovat všechno co jde)
 - Vytváøení a mazání jednotlivých komponent scénáøe
 - Zmìny poøadí jednotlivých položek
 - Vkládání nových položek do komponent scénáøe
 
Od Tyla:

SELECT
    OBJECT_NAME(parent_id) AS [Table],
    OBJECT_NAME(object_id) AS TriggerName
FROM
    sys.triggers
WHERE
    object_id = @@PROCID
Konec chatu
